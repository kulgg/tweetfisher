import { type NextPage } from "next";
import Head from "next/head";
import Link from "next/link";

const Home: NextPage = () => {
  return (
    <>
      <Head>
        <title>DeletedTweets</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center bg-gradient-to-br from-black to-gray-900">
        <h1 className="mt-20 bg-gradient-to-r from-emerald-200 to-rose-200 bg-clip-text text-7xl font-bold text-transparent">
          Find Deleted Tweets
        </h1>
        <div className="w-96 mt-20">
          <label
            htmlFor="search"
            className="block text-lg font-medium text-gray-300"
          >
            Enter a username
          </label>
          <div className="relative mt-1 flex items-center">
            <input
              type="text"
              name="search"
              id="search"
              placeholder="elonmusk"
              className="bg-neutral-900 text-gray-100 placeholder:text-gray-500 block w-full h-12 rounded-md border-gray-500 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-lg"
            />
          </div>
        </div>
      </main>
    </>
  );
};

export default Home;
