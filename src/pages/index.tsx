import { type NextPage } from "next";
import Head from "next/head";
import Link from "next/link";
import LoadingAnimation from "../components/loading";

const Home: NextPage = () => {
  const webArchiveUrl = "https://web.archive.org/cdx/search/cdx?url=twitter.com/{username}/status&matchType=prefix&filter=statuscode:200&mimetype:text/html"


  return (
    <>
      <Head>
        <title>DeletedTweets</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="min-h-screen bg-gradient-to-br from-black to-gray-900">
        <div className="container flex flex-col items-center mx-auto">
        <h1 className="mr-auto mt-20 bg-gradient-to-r from-emerald-200 to-rose-200 bg-clip-text text-5xl sm:text-7xl font-bold text-transparent">
          Find Deleted Tweets
        </h1>
        <div className="w-full mt-20">
          <label
            htmlFor="search"
            className="block text-lg font-medium text-indigo-200"
          >
            Enter a username
          </label>
          <div className="relative mt-1 flex items-center">
            <input
              type="text"
              name="search"
              autoComplete="off"
              id="search"
              placeholder="elonmusk"
              className="bg-neutral-900 text-gray-100 placeholder:text-gray-500 block w-full h-12 rounded-md border-gray-500 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-lg"
            />
          </div>
        </div>
        <div className="my-4">
        </div>
        <LoadingAnimation /> 
        </div>
      </main>
    </>
  );
};

export default Home;
