import { useQuery } from "@tanstack/react-query";
import { type NextPage } from "next";
import Head from "next/head";
import Link from "next/link";
import React, { useState } from "react";
import LoadingAnimation from "../components/loading";

const Home: NextPage = () => {
  const webArchiveUrl =
    "https://web.archive.org/cdx/search/cdx?url=twitter.com/{username}/status&matchType=prefix&filter=statuscode:200&mimetype:text/html";
  const [usernameInput, setUsernameInput] = useState("");

  const { isLoading, error, data, refetch, isFetching } = useQuery({
    queryKey: ["webarchive"],
    queryFn: () => {
      const url = `https://web.archive.org/cdx/search/cdx?url=twitter.com/${usernameInput}/status&matchType=prefix&filter=statuscode:200&mimetype:text/html`;
      console.log("url", url);
      const result = fetch(url, { mode: "no-cors" }).then((res) => res.text());
      console.log(result);
      return result;
    },
    enabled: false,
  });

  console.log(usernameInput);
  console.log("isloading", isLoading);
  console.log("data", data);

  const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    console.log("submitted");
    refetch();
    setUsernameInput("");
  };

  return (
    <>
      <Head>
        <title>DeletedTweets</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="min-h-screen bg-gradient-to-br from-black to-gray-900 text-gray-200">
        <div className="container mx-auto flex flex-col items-center">
          <h1 className="mr-auto mt-20 bg-gradient-to-r from-emerald-200 to-rose-200 bg-clip-text text-5xl font-bold text-transparent sm:text-7xl">
            Find Deleted Tweets
          </h1>
          <div className="mt-20 w-full">
            <label
              htmlFor="search"
              className="block text-lg font-medium text-indigo-200"
            >
              Enter a username
            </label>
            <form
              onSubmit={handleSubmit}
              className="relative mt-1 flex items-center"
            >
              <input
                type="text"
                name="search"
                autoComplete="off"
                id="search"
                placeholder="elonmusk"
                className="block h-12 w-full rounded-md border-gray-500 bg-neutral-900 text-lg text-gray-100 shadow-sm placeholder:text-gray-500 focus:border-[1px] focus:border-gray-300 focus:ring-1 focus:ring-emerald-700"
                value={usernameInput}
                onChange={(e) => setUsernameInput(e.currentTarget.value)}
              />
            </form>
          </div>
          <div className="my-4"></div>
          {isFetching && <LoadingAnimation />}
          {data}
        </div>
      </main>
    </>
  );
};

export default Home;
